/**
 * Generate a move based on existing game state
 * and previous move
 * @param {GameState} game_state 
 * @param {Move} last_move 
 * @returns {Move}
 */
function move(game_state, last_move) {
    console.log("AI Easy Moved")
}

export default move;